/** Server tick rate in Hz */
export const TICK_RATE = 20;

/** Area-of-interest chunk radius matching the client's render radius. */
export const AOI_CHUNK_RADIUS = 2;

/** Tiles per chunk side */
export const CHUNK_SIZE = 32;

/** Player movement speed in tiles per second */
export const MOVE_SPEED = 4;

/** Max hours before offline cap is applied */
export const OFFLINE_CAP_HOURS = 24;

/** Max items per trade */
export const MAX_TRADE_ITEMS = 10;

/** Max input items for crafting */
export const MAX_CRAFT_INPUTS = 3;

/** Default world generation seed shared between client and server. */
export const DEFAULT_SEED = 20260825;
